import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 5,
  },
  headerText: {
    fontSize: 25,
    color: '#bbb',
    marginTop: 20,
    textAlign: 'center',
  },
});
export default styles;
